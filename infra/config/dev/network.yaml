template:
  path: network.j2
  type: file

parameters:
  VpcId: !stack_attr sceptre_user_data.vpc_id
  PrivateSubnetACidr: "{{ var.private_subnet_a_cidr }}"
  PrivateSubnetBCidr: "{{ var.private_subnet_b_cidr }}"
