region: "{{ var.region }}"
project_code: comet

# Environment and Service are injected because the bootstrap IAM policies require these exact values.
stack_tags: {{ dict(var.stack_tags, Environment=var.env, Service='comet') | tojson }}
